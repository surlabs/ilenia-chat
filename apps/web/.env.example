NEXT_PUBLIC_SERVER_URL=
# Internal server URL used by Next.js rewrites (server-side only).
# In Docker production set this to the internal service name: http://server:3000
# In local dev leave unset — falls back to NEXT_PUBLIC_SERVER_URL.
SERVER_INTERNAL_URL=